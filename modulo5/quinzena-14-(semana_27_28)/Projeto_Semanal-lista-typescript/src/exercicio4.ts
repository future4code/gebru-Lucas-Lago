// Exercício 4

type arrayColaboradores = {
    nome: string,
    salario: number,
    setor: SETOR,
    presencial: boolean
}

enum SETOR {
    MARKETING = "marketing",
    VENDAS = "vendas",
    FINANCEIRO = "financeiro",
}

export function pessoasSelecionadas(
    arrayColaboradores: { nome: string, salario: number, setor: SETOR, presencial: boolean}
) :void {

}