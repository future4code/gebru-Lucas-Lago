import React from "react";
import {ContainerHeader, ButtonHeader} from "./StylesHeader"

const Header = (props) => {
    return(
        <ContainerHeader>
            <h1> Labefy </h1>
        </ContainerHeader>
    )
}

export default Header